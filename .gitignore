.DS_Store
/Configurations/custom.xcconfig
.swiftpm
RuntimeViewerUsingAppKit/RuntimeViewerCatalystHelper.app
archive
build
Archives
xcuserdata
.claude/settings.local.json
RuntimeViewerUsingAppKit/Packaging.log
RuntimeViewerUsingAppKit/ExportOptions.plist
RuntimeViewerUsingAppKit/DistributionSummary.plist
.claude
buildServer.json
.codex
Products
RuntimeViewerMCP/.build

# Tuist (proposal 0012). Tuist/Package.swift and Tuist/Package.resolved are
# tracked — the resolved file is the dependency lock for the external
# integration. Everything Tuist fetches or derives underneath is not.
Tuist/.build
Tuist/Dependencies
Derived/

# Tuist-generated projects for the local packages. The hand-written app
# projects under RuntimeViewerUsingAppKit / RuntimeViewerServer /
# RuntimeViewerUsingUIKit stay tracked; only these generated ones are ignored.
RuntimeViewerCore/RuntimeViewerCore.xcodeproj
RuntimeViewerCore/RuntimeViewerCore.xcworkspace
RuntimeViewerCore/.package.resolved
RuntimeViewerPackages/RuntimeViewerPackages.xcodeproj
RuntimeViewerPackages/RuntimeViewerPackages.xcworkspace
RuntimeViewerPackages/.package.resolved
RuntimeViewerMCP/RuntimeViewerMCP.xcodeproj
RuntimeViewerMCP/RuntimeViewerMCP.xcworkspace
RuntimeViewerMCP/.package.resolved
DerivedData
.derived-data-log-*
.omc
*.env
RuntimeViewer.xcworkspace/xcshareddata/swiftpm

# Local checkouts of upstream libraries (git worktrees), used when
# USING_LOCAL_DEPENDENCIES=1. Must live inside the project root for Tuist to
# resolve them as .package(path:).
LocalDependencies/
